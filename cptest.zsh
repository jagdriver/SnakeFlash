#!/bin/bash

#mkdir "${boot}/Stacks"
    #for f in ../../Docker/Stacks/*; do
    #cp -r "$f" "${boot}/Stacks"
    #done
    pwd
    ls -l
    ls -l ../SnakeFlash/Stacks
    ls -l ../../Docker/Stacks
    ls -l "${boot}/Stacks"